from setuptools import setup, find_packages

setup(
    name="mylibrary",
    version="1.0.0",
    author="Amin",
    description="Library Management Package",
    packages=find_packages(),
    install_requires=[],
)